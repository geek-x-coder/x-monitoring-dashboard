# 개발 모드 환경 변수 (Vite는 `vite` / `npm run dev` 실행 시 자동으로 이 파일을 로드함)
# 우선순위: .env.development.local > .env.development > .env.local > .env

# 백엔드 API 주소 (로컬 개발용)
VITE_API_URL=http://127.0.0.1:5000

# 자동 갱신 주기 (ms)
VITE_REFRESH_INTERVAL=5000

# 디버그 모드
VITE_DEBUG=true

# 프론트엔드 버전
VITE_APP_VERSION=2.0.0
